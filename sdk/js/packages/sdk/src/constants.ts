/** /health 响应中标识服务的应用名,与服务端 api::APP_ID 一致 */
export const APP_ID = 'x-notify-service'

/** 服务默认起始端口(与服务端 DEFAULT_PORT 一致) */
export const DEFAULT_BASE_PORT = 17320

/** 端口探测区间长度(与服务端向后探测数量一致) */
export const DEFAULT_PORT_RANGE = 10

/** 协议拉起地址 */
export const PROTOCOL_URL = 'x-notify://launch'
